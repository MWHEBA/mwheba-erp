# 📘 دليل توحيد واجهة المستخدم - MWHEBA ERP

> **الدليل الشامل لتوحيد جميع الصفحات والمكونات في النظام**

**المرجعية الوحيدة:** راجع هذا الملف دائماً قبل تعديل أي صفحة

**مثال حي:** [http://127.0.0.1:8000/sales/](http://127.0.0.1:8000/sales/) - الصفحة المرجعية للتصميم الموحد

---

## 📋 جدول المحتويات

1. [الهدف والمبادئ](#-الهدف-والمبادئ)
2. [الملفات المركزية](#-الملفات-المركزية)
3. [Checklist: توحيد صفحة جديدة](#-checklist-توحيد-صفحة-جديدة)
4. [المكونات المشتركة](#-المكونات-المشتركة)
5. [مثال صفحة كاملة](#-مثال-صفحة-كاملة)
6. [أخطاء شائعة وحلولها](#-أخطاء-شائعة-وحلولها)
7. [تعديل الستايل](#-تعديل-الستايل)

---

## 🎯 الهدف والمبادئ

### الهدف
توحيد جميع الصفحات لتستخدم:
- ✅ **Header و Breadcrumb مركزي** من `shared/page_header.html`
- ✅ **Stats Cards موحدة** من `shared-components.css`
- ✅ **لا inline CSS** - كل الستايلات في ملفات منفصلة
- ✅ **عملة ديناميكية** من الإعدادات (`{% currency_symbol %}`)
- ✅ **CSS Variables فقط** - لا ألوان hardcoded

### المبادئ الأساسية
1. **تعديل واحد يطبق على الكل** - كل مكون في مكان واحد
2. **لا تكرار** - استخدم القوالب المشتركة دائماً
3. **المرجعية في View** - كل البيانات تُحدد في View، ليس في Template
4. **CSS Variables** - استخدم `var(--primary-color)` بدل `#007bff`
5. **🔴 تعديل واحد لكل ملف** - لا تعدل نفس الملف أكثر من مرة
6. **🔴 الأزرار في الهيدر** - جميع الأزرار (تنقل + وظيفية) في `header_buttons`

---

## 📁 الملفات المركزية

### 1. ملف CSS المركزي
**الموقع:** `static/css/shared-components.css`

يحتوي على جميع ستايلات:
- Breadcrumb (البريدكرمب)
- Page Headers (هيدر الصفحات)
- Stats Cards (كروت الإحصائيات)

**✅ تم تحميله تلقائياً في `base.html`**

### 2. القوالب المركزية
**الموقع:** `templates/shared/`

- `breadcrumb.html` - البريدكرمب
- `page_header.html` - هيدر الصفحة (يتضمن breadcrumb)

### 3. خريطة ملفات CSS

| الستايل | الملف المناسب |
|---------|---------------|
| Breadcrumb, Header, Stats Cards | `shared-components.css` ✅ |
| section-container, empty-state | `components.css` |
| Tables, pagination, filters | `tables.css` |
| Sidebar, layout | `layout.css` |
| CSS Variables, utilities | `main.css` |

**⚠️ قاعدة ذهبية:** عدل المكونات المشتركة في `shared-components.css` فقط!

---

## 📋 Checklist: توحيد صفحة جديدة

### ⚠️ قبل ما تبدأ - اقرأ الصفحة الأصلية كويس!

**خطوات إلزامية قبل التعديل:**

1. **🔴 ابعت لينك الصفحة في سطر لوحده** - قبل أي تعديل
   - **مثال:**
   ```
   http://127.0.0.1:8000/products/stock/
   ```
   - **ممنوع** تضيف أي كلام قبل أو بعد اللينك
   - اللينك لوحده في سطر منفصل عشان سهولة النسخ

2. **افتح الصفحة في المتصفح** - شوف شكلها الأصلي
3. **اقرأ الـ Template كامل** - افهم كل حاجة فيها
4. **حدد الأزرار الموجودة** - اكتبهم في ورقة جنبك
5. **حدد الـ CSS الخاص** - لو فيه ستايلات مهمة (زي stock.css)
6. **افتح الـ View** - شوف الـ context الحالي

**⛔ لو مش متأكد - متعدلش!**
**🔴 ممنوع التعديل قبل إرسال اللينك للمراجعة!**

---

### 1️⃣ في الـ **View** (views.py)

أضف هذه المتغيرات في الـ `context`:

```python
from django.urls import reverse

def my_page_view(request):
    # حساب البيانات...
    
    context = {
        # ✅ مطلوب: بيانات الهيدر
        "page_title": "عنوان الصفحة",              # ✅ مطلوب
        "page_subtitle": "وصف الصفحة",             # ✅ مطلوب
        "page_icon": "fas fa-icon-name",           # ✅ مطلوب
        
        # ✅ اختياري: Badges تحت العنوان (للتفاصيل السريعة)
        "header_badges": [                         # ✅ اختياري
            {
                "text": "TECH001",  # الأيقونة هتظهر تلقائياً
                "class": "bg-primary",
                "icon": "fas fa-hashtag",
            },
            {
                "text": "المديونية: 5000",
                "class": "bg-danger",
                "icon": "fas fa-arrow-up",
            },
            {
                "text": "دليل الحسابات",
                "class": "bg-success",
                "icon": "fas fa-link",
                "url": reverse("financial:account_detail", kwargs={"pk": 123}),  # ✅ badge قابل للنقر
            },
            {
                "text": "إنشاء حساب",
                "class": "bg-info",
                "icon": "fas fa-plus-circle",
                "onclick": "openModal()",  # ✅ badge مع JavaScript
            },
            {
                "text": "طباعة",
                "class": "badge",  # ✅ استخدام class عادي
                "icon": "fas fa-print",
                "style": "background-color: #ff6b6b; color: white;",  # ✅ ألوان مخصصة
            },
        ],
        
        # ✅ اختياري: أزرار الهيدر (3 أنواع مدعومة)
        "header_buttons": [                        # ✅ اختياري
            # النوع 1: زر تنقل (url)
            {
                "url": reverse("app:action"),
                "icon": "fa-plus",
                "text": "إضافة",
                "class": "btn-primary",
            },
            # النوع 2: زر وظيفي (onclick) - للطباعة والتصدير
            {
                "onclick": "window.print()",
                "icon": "fa-print",
                "text": "طباعة",
                "class": "btn-success",
            },
            # النوع 3: زر نموذج (form_id)
            {
                "form_id": "my-form",
                "icon": "fa-save",
                "text": "حفظ",
                "class": "btn-primary",
            }
        ],
        
        # ⚠️ قاعدة مهمة: أزرار Export/Print/Sync تُضاف في header_buttons
        # ❌ لا تضعها خارج الهيدر في الصفحة
        # ✅ استخدم onclick للأزرار الوظيفية (print, export)
        
        # ✅ اختياري: البريدكرمب (أو سيتم توليده تلقائياً)
        "breadcrumb_items": [                      # ✅ اختياري
            {"title": "الرئيسية", "url": reverse("core:dashboard"), "icon": "fas fa-home"},
            {"title": "الصفحة الحالية", "active": True},
        ],
        
        # بيانات الصفحة الأخرى...
    }
    
    return render(request, "app/my_page.html", context)
```

#### ✅ استبدله بسطر واحد:
```django
{% extends 'base.html' %}
{% load static %}
{% load utils_extras %}  <!-- للقيم المالية -->

{% block content %}
<div class="container-fluid">
    <!-- Page Header المركزي -->
    {% include "shared/page_header.html" %}
    
    <!-- باقي محتوى الصفحة -->
</div>
{% endblock %}
```

---

#### ب. مع محتوى إضافي في الهيدر (صفحات التفاصيل)

**مثال:** صفحة تفاصيل العميل تحتاج badges إضافية تحت الهيدر

```django
{% block content %}
<div class="container-fluid">
    <!-- Page Header المركزي -->
    {% include "shared/page_header.html" %}
    
    {% block extra_header_content %}
    <!-- محتوى إضافي خاص بالصفحة -->
    <div class="custom-header">
        <div class="badges">
            <span class="badge bg-primary">{{ customer.code }}</span>
            <span class="badge bg-success">{{ customer.balance }}</span>
        </div>
    </div>
    {% endblock %}
    
    <!-- باقي محتوى الصفحة -->
</div>
{% endblock %}
```

**ملاحظة:** الـ `extra_header_content` block يظهر **داخل** الـ `page-header-container` مباشرة تحت الهيدر الأساسي.

---

### 3️⃣ الكروت (Stats Cards)

#### ⚠️ ملاحظات مهمة:
- **للقيم المالية:** استخدم `{% currency_symbol %}` بدل "جنيه" أو "ريال"
- **تحميل utils_extras:** لازم تضيف `{% load utils_extras %}` في أول الـ template
- **استخدم HTML مباشر** - لا تستخدم include للكروت

#### ✅ HTML الكروت:
```django
<div class="row">
    <!-- كارت عادي -->
    <div class="col-xl-3 col-md-6 mb-4">
        <div class="stats-card stats-card-primary">
            <div class="stats-card-header"></div>
            <div class="stats-card-body">
                <div class="stats-card-icon">
                    <i class="fas fa-shopping-cart"></i>
                </div>
                <div class="stats-card-title">إجمالي الفواتير</div>
                <div class="stats-card-value">{{ sales_count|default:0 }}</div>
                <div class="stats-card-unit">فاتورة</div>
            </div>
        </div>
    </div>
    
    <!-- كارت بقيمة مالية -->
    <div class="col-xl-3 col-md-6 mb-4">
        <div class="stats-card stats-card-success">
            <div class="stats-card-header"></div>
            <div class="stats-card-body">
                <div class="stats-card-icon">
                    <i class="fas fa-money-bill-wave"></i>
                </div>
                <div class="stats-card-title">إجمالي المبيعات</div>
                <div class="stats-card-value">{{ total_amount|smart_float:2 }}</div>
                <div class="stats-card-unit">{% currency_symbol %}</div>
            </div>
        </div>
    </div>
</div>
```

#### 🎨 ألوان الكروت المتاحة:
- `stats-card-primary` - أزرق
- `stats-card-success` - أخضر
- `stats-card-danger` - أحمر
- `stats-card-warning` - برتقالي
- `stats-card-info` - سماوي

---

### 4️⃣ تنظيف الـ inline CSS

#### ❌ احذف جميع الـ inline styles للمكونات المشتركة:
```django
{% block extra_css %}
<style>
  /* ❌ احذف كل هذا - موجود في shared-components.css */
  .stats-card { ... }
  .stats-card-primary { ... }
  .section-container { ... }
  .section-title { ... }
  .breadcrumb { ... }
  .page-header { ... }
  
  /* ❌ احذف كل هذا - موجود في components.css */
  .empty-state { ... }
  .soft-badge { ... }
  
  /* ❌ احذف كل هذا - موجود في tables.css */
  .action-button { ... }
  .pagination { ... }
</style>
{% endblock %}
```

#### ✅ استبدله بـ:
```django
{% block extra_css %}
<!-- جميع الستايلات موجودة في shared-components.css و components.css -->
<style>
  /* ✅ فقط الستايلات الخاصة بهذه الصفحة */
  .page-specific-class { ... }
</style>
{% endblock %}
```

**أو إذا مفيش ستايلات خاصة:**
```django
{% block extra_css %}
<!-- جميع الستايلات موجودة في shared-components.css و components.css -->
{% endblock %}
```

---

## 🧩 المكونات المشتركة

### 1. Breadcrumb

#### الطريقة البسيطة (تلقائي)
```django
{% include "shared/breadcrumb.html" %}
```
سيستخدم `generated_breadcrumb_items` تلقائياً من context processor

#### الطريقة المخصصة (في View)
```python
def my_view(request):
    breadcrumb_items = [
        {'title': 'الرئيسية', 'url': '/', 'icon': 'fa-home'},
        {'title': 'العملاء', 'url': '/clients/', 'icon': 'fa-users'},
        {'title': 'إضافة عميل', 'active': True}
    ]
    return render(request, 'my_template.html', {'breadcrumb_items': breadcrumb_items})
```

---

### 2. Page Header

**⚠️ القاعدة الذهبية:** لا تمرر parameters في الـ include! كل حاجة في الـ **View**

#### ❌ خطأ شائع:
```django
{% include "shared/page_header.html" with title="..." subtitle="..." %}
```

#### ✅ الطريقة الصحيحة:

**في Template:**
```django
{% include "shared/page_header.html" %}
```

**في View:**
```python
context = {
    "page_title": "قائمة العملاء",
    "page_subtitle": "إدارة بيانات العملاء والموردين",
    "page_icon": "fas fa-users",
    "header_buttons": [
        {
            "url": reverse("client:customer_create"),
            "icon": "fa-plus",
            "text": "إضافة عميل",
            "class": "btn-primary",
        }
    ],
}
```

---

### 3. Stats Cards

**⚠️ استخدم HTML مباشر - لا include**

#### مثال كارت بسيط
```django
<div class="col-xl-3 col-md-6 mb-4">
    <div class="stats-card stats-card-primary">
        <div class="stats-card-header"></div>
        <div class="stats-card-body">
            <div class="stats-card-icon">
                <i class="fas fa-users"></i>
            </div>
            <div class="stats-card-title">إجمالي العملاء</div>
            <div class="stats-card-value">{{ customers_count|default:0 }}</div>
            <div class="stats-card-unit">عميل</div>
        </div>
    </div>
</div>
```

#### مثال كارت بقيمة مالية
```django
<div class="col-xl-3 col-md-6 mb-4">
    <div class="stats-card stats-card-success">
        <div class="stats-card-header"></div>
        <div class="stats-card-body">
            <div class="stats-card-icon">
                <i class="fas fa-money-bill-wave"></i>
            </div>
            <div class="stats-card-title">إجمالي المبيعات</div>
            <div class="stats-card-value">{{ total_amount|smart_float:2 }}</div>
            <div class="stats-card-unit">{% currency_symbol %}</div>
        </div>
    </div>
</div>
```

---

## 🎯 مثال صفحة كاملة

### ✅ View (sale/views.py):
```python
from django.shortcuts import render
from django.urls import reverse
from django.contrib.auth.decorators import login_required
from django.db.models import Sum

@login_required
def sale_list(request):
    # جلب البيانات
    sales = Sale.objects.all().order_by("-date", "-id")
    sales_count = sales.count()
    total_amount = sales.aggregate(Sum('total'))['total__sum'] or 0
    
    # تحضير الـ context
    context = {
        "sales": sales,
        "sales_count": sales_count,
        "total_amount": total_amount,
        
        # بيانات الهيدر
        "page_title": "فواتير المبيعات",
        "page_subtitle": "قائمة بجميع فواتير المبيعات في النظام",
        "page_icon": "fas fa-shopping-cart",
        
        # أزرار الهيدر
        "header_buttons": [
            {
                "url": reverse("sale:sale_create"),
                "icon": "fa-plus",
                "text": "إضافة فاتورة",
                "class": "btn-primary",
            }
        ],
        
        # البريدكرمب
        "breadcrumb_items": [
            {"title": "الرئيسية", "url": reverse("core:dashboard"), "icon": "fas fa-home"},
            {"title": "المبيعات", "url": "#", "icon": "fas fa-truck"},
            {"title": "فواتير المبيعات", "active": True},
        ],
    }
    
    return render(request, "sale/sale_list.html", context)
```

### ✅ Template (sale/sale_list.html):
```django
{% extends 'base.html' %}
{% load static %}
{% load i18n %}
{% load utils_extras %}

{% block title %}{{ page_title }}{% endblock %}

{% block extra_css %}
<!-- جميع الستايلات موجودة في components.css -->
{% endblock %}

{% block content %}
<div class="container-fluid">
    <!-- Page Header المركزي -->
    {% include "shared/page_header.html" %}
    
    <!-- الإحصائيات -->
    <div class="section-container">
        <h5 class="section-title">
            <i class="fas fa-chart-bar me-2"></i>{% trans "الإحصائيات العامة" %}
        </h5>
        <div class="row">
            <div class="col-xl-3 col-md-6 mb-4">
                <div class="stats-card stats-card-primary">
                    <div class="stats-card-header"></div>
                    <div class="stats-card-body">
                        <div class="stats-card-icon">
                            <i class="fas fa-shopping-cart"></i>
                        </div>
                        <div class="stats-card-title">إجمالي الفواتير</div>
                        <div class="stats-card-value">{{ sales_count|default:0 }}</div>
                        <div class="stats-card-unit">فاتورة</div>
                    </div>
                </div>
            </div>
            
            <div class="col-xl-3 col-md-6 mb-4">
                <div class="stats-card stats-card-success">
                    <div class="stats-card-header"></div>
                    <div class="stats-card-body">
                        <div class="stats-card-icon">
                            <i class="fas fa-money-bill-wave"></i>
                        </div>
                        <div class="stats-card-title">إجمالي المبيعات</div>
                        <div class="stats-card-value">{{ total_amount|smart_float:2 }}</div>
                        <div class="stats-card-unit">{% currency_symbol %}</div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <!-- الجدول -->
    <div class="section-container">
        <!-- محتوى الجدول -->
    </div>
</div>
{% endblock %}
```

### 📊 النتيجة:
- ❌ **قبل:** 198 سطر (Breadcrumb + Header + CSS inline)
- ✅ **بعد:** 59 سطر فقط
- 🎉 **التوفير:** 74% من الكود!

---

## ⚠️ أخطاء شائعة وحلولها

### ❌ 1. تمرير parameters في include
```django
<!-- ❌ خطأ -->
{% include "shared/page_header.html" with title="..." subtitle="..." %}
```
**✅ الحل:** حدد كل حاجة في الـ **View** مش في الـ template

---

### ❌ 2. خلط أزرار الهيدر مع أزرار الجدول
```python
# ❌ خطأ - استخدام نفس المتغير
context = {
    "action_buttons": [...],  # أزرار الجدول
    # نسيت header_buttons للهيدر!
}
```
**✅ الحل:** افصل الأزرار
```python
context = {
    "header_buttons": [...],   # أزرار الهيدر (إضافة، مزامنة، إلخ)
    "action_buttons": [...],   # أزرار الجدول (عرض، تعديل، حذف)
}
```

---

### ❌ 3. نسيان page_subtitle
```python
# ❌ خطأ
context = {
    "page_title": "...",
    # نسيت page_subtitle
}
```
**✅ الحل:** أضف `page_subtitle` دائماً

---

### ❌ 4. استخدام filters في include
```django
<!-- ❌ خطأ -->
{% include "shared/stats_card.html" with value=sales|length %}
```
**✅ الحل:** احسب القيمة في الـ View أو استخدم HTML مباشر

---

### ❌ 5. استخدام "جنيه" بدل currency_symbol
```django
<!-- ❌ خطأ -->
<div class="stats-card-unit">جنيه</div>
```
**✅ الحل:** استخدم `{% currency_symbol %}`

---

### ❌ 6. نسيان {% load utils_extras %}
```django
<!-- ❌ خطأ -->
{% extends 'base.html' %}
{% load static %}
<!-- نسيت load utils_extras -->

{{ total|smart_float:2 }}  <!-- لن يشتغل -->
{% currency_symbol %}       <!-- لن يشتغل -->
```
**✅ الحل:** أضف `{% load utils_extras %}` في البداية

---

### ❌ 7. ترك inline CSS للمكونات المشتركة
```django
<!-- ❌ خطأ - ستايلات مكررة -->
{% block extra_css %}
<style>
  .stats-card { ... }
  .section-container { ... }
  .breadcrumb { ... }
  .action-button { ... }
</style>
{% endblock %}
```
**✅ الحل:** امسح كل الستايلات المشتركة - موجودة في الملفات المركزية

**القاعدة الذهبية:**
- ✅ **احتفظ فقط** بالستايلات الخاصة بالصفحة نفسها
- ❌ **امسح نهائياً** ستايلات: الكروت، الهيدر، البريدكرمب، الجداول، الأزرار

**⚠️ استثناءات معروفة - لا تمسح:**
```django
{% block extra_css %}
<link rel="stylesheet" href="{% static 'css/stock.css' %}">  <!-- ✅ للـ stock indicators -->
{% endblock %}
```

**مثال صحيح:**
```django
{% block extra_css %}
<!-- جميع الستايلات موجودة في shared-components.css و components.css -->
<style>
  /* ستايلات خاصة بهذه الصفحة فقط */
  .supplier-type-badge { ... }  /* ✅ خاص بالموردين فقط */
</style>
{% endblock %}
```

---

### ❌ 8. استخدام ألوان hardcoded
```css
/* ❌ خطأ */
.my-element {
    background-color: #007bff;
    color: #ffffff;
}
```
**✅ الحل:** استخدم CSS variables
```css
.my-element {
    background-color: var(--primary-color);
    color: var(--white);
}
```

---

## 🔧 تعديل الستايل

### ⚠️ قاعدة ذهبية:

**المرجعية الوحيدة للستايل:**
```
static/css/shared-components.css فقط!
```

**❌ لا تعدل في:**
- `main.css` ← تم إزالة جميع ستايلات المكونات المشتركة منه
- `components.css` ← فقط للـ breadcrumb العام (تم تحديثه)
- أي ملف آخر

### لتعديل ستايل أي مكون:

1. **افتح الملف:** `static/css/shared-components.css`
2. **ابحث عن القسم المطلوب:**
   - `/* BREADCRUMB STYLES */`
   - `/* PAGE HEADER STYLES */`
   - `/* STATS CARDS STYLES */`
3. **عدل الستايل المطلوب**
4. **احفظ الملف**

✅ **التعديل سيطبق تلقائياً على جميع الصفحات**
✅ **لا تعارض ولا ازدواجية**

### أمثلة تعديلات شائعة:

#### تغيير حجم عنوان الصفحة
```css
.header-title {
    font-size: 2rem; /* بدلاً من 1.75rem */
}
```

#### تغيير لون Breadcrumb
```css
.page-breadcrumb .breadcrumb-item a {
    color: var(--primary-color); /* استخدم CSS variable */
}
```

#### تغيير حجم الكروت
```css
.stats-card {
    border-radius: 15px; /* بدلاً من 10px */
}
```

---

## ✅ Checklist نهائي - اتبعه بالترتيب!

### 🔴 المرحلة 1: قبل التعديل (إلزامي!)

- [ ] **🔴 بعتت لينك الصفحة في سطر لوحده** - ممنوع التعديل قبل كده!
  - اللينك لوحده بدون أي كلام قبله أو بعده
  - مثال صحيح:
  ```
  http://127.0.0.1:8000/products/stock/
  ```
- [ ] **افتحت الصفحة في المتصفح** وشوفت شكلها الأصلي
- [ ] **قريت الـ Template كامل** من أول سطر لآخر سطر
- [ ] **كتبت كل الأزرار الموجودة** في ورقة جنبك:
  - أزرار الهيدر (إضافة، تعديل، إلخ)
  - أزرار وظيفية (Export, Print, Sync)
  - أزرار الجدول (عرض، تعديل، حذف)
- [ ] **حددت الـ CSS الخاص** - لو فيه ملفات CSS مهمة (stock.css مثلاً)
- [ ] **فتحت الـ View** وشوفت الـ context الحالي

**⛔ لو أي نقطة مش متأكد منها - متكملش!**
**🔴 لو مبعتتش اللينك - ممنوع تعدل!**

---

### 🟡 المرحلة 2: التعديل في الـ View
---

### 🟢 المرحلة 3: التعديل في الـ Template

- [ ] ✅ أضفت `{% load utils_extras %}` في أول الملف
- [ ] ✅ استبدلت الهيدر القديم بـ `{% include "shared/page_header.html" %}`
- [ ] ✅ **راجعت الـ CSS:**
  - [ ] حددت الـ CSS الخاص بالصفحة (زي stock.css)
  - [ ] مسحت كل الـ inline CSS المكرر (stats-card, breadcrumb, إلخ)
  - [ ] **سيبت** الـ CSS الخاص بالصفحة (لو موجود)
- [ ] ✅ **ماكسرتش حاجة:**
  - [ ] الفلاتر لسه موجودة
  - [ ] الجدول لسه موجود
  - [ ] الـ JavaScript لسه موجود
  - [ ] الأزرار الوظيفية لسه موجودة

---

### 🔵 المرحلة 4: الاختبار (إلزامي!)

- [ ] ✅ **فتحت الصفحة في المتصفح** - شغالة بدون أخطاء
- [ ] ✅ **البريدكرمب** ظاهر صح
- [ ] ✅ **الهيدر** فيه:
  - العنوان الصحيح
  - الوصف الصحيح
  - الأيقونة الصحيحة
  - **كل الأزرار اللي كانت موجودة قبل كده**
- [ ] ✅ **جربت كل زرار** - كل زرار بيودي للرابط الصحيح
- [ ] ✅ **الفلاتر شغالة** (لو موجودة)
- [ ] ✅ **الجدول شغال** (لو موجود)
- [ ] ✅ **الأزرار الوظيفية شغالة** (Export, Print, إلخ)
- [ ] ✅ **الشكل مطابق** للصفحة القديمة
- [ ] ✅ **responsive** على mobile

---

### ⚠️ قبل ما تعمل Commit:

**🔴 قاعدة مهمة: اعمل 3 صفحات كل مرة، ثم حدث التقرير مرة واحدة**

**📋 في بداية كل مجموعة 3 صفحات:**

**🔴 CRITICAL - التأكد من صحة الروابط:**
- [ ] ✅ **ابحث عن URLs الصحيحة في `urls.py`** - لا تخمن الروابط!
- [ ] ✅ **افتح كل رابط في المتصفح** - تأكد إنه يفتح بدون 404
- [ ] ✅ **استخدم أمثلة حقيقية** - لو الصفحة محتاجة ID، استخدم ID موجود فعلاً
- [ ] ✅ **ابعت الروابط المؤكدة** في بداية الرسالة (كل رابط في سطر لوحده):
  ```
  http://127.0.0.1:8000/path/to/page1/
  http://127.0.0.1:8000/path/to/page2/
  http://127.0.0.1:8000/path/to/page3/
  ```

**⚠️ أمثلة على روابط صحيحة وخاطئة:**
- ❌ **خطأ:** `http://127.0.0.1:8000/client/1/` (مفيش view بالاسم ده)
- ✅ **صح:** `http://127.0.0.1:8000/client/customer/1/` (من urls.py)
- ❌ **خطأ:** `http://127.0.0.1:8000/supplier/form/` (مفيش view بالاسم ده)
- ✅ **صح:** `http://127.0.0.1:8000/supplier/add/` (من urls.py)

**🔍 كيف تتأكد من الرابط الصحيح:**
1. افتح ملف `urls.py` للوحدة
2. ابحث عن `path()` للـ view المطلوب
3. اجمع المسار الكامل من `app_name` + `path`
4. لو محتاج ID، استخدم ID موجود (مثلاً: 1)
5. افتح الرابط في المتصفح للتأكد

---

## 🚨 قواعد مهمة للأزرار في الهيدر

### 👉 التزم بالهيدر الأصلي!

**⚠️ قاعدة ذهبية:** لو الهيدر الأصلي فيه زرار جوا الهيدر → حطهم في `header_buttons`

**✅ مثال صحيح:**

لو الهيدر الأصلي فيه:
```html
<div class="page-header">
    <h1>قائمة الموظفين</h1>
    <div>
        <button data-bs-toggle="modal">استيراد</button>
        <a href="/add/">إضافة</a>
    </div>
</div>
```

يبقى في الـ View:
```python
'header_buttons': [
    {
        'url': '#',
        'icon': 'fa-file-import',
        'text': 'استيراد',
        'class': 'btn-outline-success',
        'toggle': 'modal',
        'target': '#importModal',
    },
    {
        'url': reverse('hr:employee_form'),
        'icon': 'fa-plus',
        'text': 'إضافة موظف',
        'class': 'btn-primary',
    },
]
```

**❌ غلط شائع:** 
متحطش الزرار تحت الهيدر لو كانوا جوا الهيدر الأصلي!

### 📝 متى تستخدم `toggle` و `target`:

لو الزر بيفتح مودال:
```python
{
    'url': '#',  # مهم!
    'toggle': 'modal',
    'target': '#modalId',
    # ...
}
```

لو الزر بيعمل navigation عادي:
```python
{
    'url': reverse('app:view_name'),
    # مفيش toggle ولا target
}
```

- [ ] ✅ **راجع الأزرار والوظائف** في كل صفحة

**بعد الانتهاء من الـ 3 صفحات:**
- [ ] ✅ **خلصت 3 صفحات** - لا تحدث التقرير قبل كده
- [ ] ✅ **قارنت كل صفحة بالقديمة** - نفس الشكل تماماً
- [ ] ✅ **جربت كل حاجة في الـ 3 صفحات** - مفيش حاجة مكسورة
- [ ] ✅ **راجعت الأزرار في كل صفحة** - كلها شغالة ونفس الروابط
- [ ] ✅ **راجعت الـ CSS في كل صفحة** - مفيش CSS مهم اتمسح
- [ ] ✅ **حدثت التقدم في `docs/PAGES_TODO.md`** - مرة واحدة للـ 3 صفحات بـ MultiEdit:
  - تحديث الإحصائيات (منتهي، متبقي)
  - وضع ✅ على الـ 3 صفحات مع اللينكات
  - تحديث نسبة الوحدة
  - تحديث الجدول النهائي

**⛔ لو أي نقطة مش متحققة - متعملش Commit!**

---

## 📱 Responsive Design

جميع المكونات responsive تلقائياً:

- **Desktop:** عرض كامل مع جميع التفاصيل
- **Tablet:** تعديل تلقائي للمسافات
- **Mobile:** 
  - الكروت تصبح عمود واحد
  - الهيدر يتحول لعمودي
  - الأزرار تأخذ العرض الكامل

---

## 🔄 ترحيل الصفحات القديمة

### قبل:
```django
<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="/">الرئيسية</a></li>
        <li class="breadcrumb-item active">العملاء</li>
    </ol>
</nav>

<div class="page-header">
    <h1>قائمة العملاء</h1>
    <p>وصف الصفحة</p>
</div>

<div class="stats-cards">
    <!-- كروت مكررة -->
</div>
```

### بعد:
```django
{% include "shared/page_header.html" %}

<div class="row">
    <!-- كروت موحدة -->
</div>
```

---

## 🎉 النتائج المتوقعة

بعد توحيد جميع الصفحات:

- ✅ **كود أقل** - توفير 50-75% من الكود
- ✅ **صيانة أسهل** - تعديل في مكان واحد يطبق على الكل
- ✅ **شكل موحد** - جميع الصفحات بنفس التصميم

---

## 📚 أمثلة واقعية من الصفحات الموحدة

### مثال 1: product_list.html
**الرابط:** http://127.0.0.1:8000/products/

**View:**
```python
context = {
    "page_title": "المنتجات",
    "page_subtitle": "إدارة وعرض جميع المنتجات",
    "page_icon": "fas fa-boxes",
    "header_buttons": [
        {
            "url": reverse("product:product_create"),
            "icon": "fa-plus",
            "text": "إضافة منتج",
            "class": "btn-primary",
        },
    ],
}
```

### مثال 2: stock_list.html (مع CSS خاص)
**الرابط:** http://127.0.0.1:8000/products/stock/

**Template:**
```django
{% block extra_css %}
<link rel="stylesheet" href="{% static 'css/stock.css' %}">  <!-- ✅ للـ stock indicators -->
{% endblock %}
```

**ملحوظة:** هذه الصفحة تحتاج stock.css للـ stock indicators - لا تمسحه!

### مثال 3: warehouse_list.html
**الرابط:** http://127.0.0.1:8000/products/warehouses/

**View:**
```python
context = {
    "page_title": "المخازن",
    "page_subtitle": "إدارة المخازن ومواقع التخزين",
    "page_icon": "fas fa-warehouse",
    "header_buttons": [
        {
            "url": reverse("product:warehouse_create"),
            "icon": "fa-plus",
            "text": "إضافة مخزن",
            "class": "btn-success",
        },
    ],
}
```
- ✅ **أداء أفضل** - CSS يتم cache
- ✅ **لا تكرار** - كل حاجة في مكان واحد
- ✅ **مرجعية واضحة** - [http://127.0.0.1:8000/sales/](http://127.0.0.1:8000/sales/)

---

## 📞 الدعم

في حالة وجود أي استفسارات أو مشاكل:
1. راجع هذا الدليل أولاً
2. تحقق من الأمثلة في `templates/shared/`
3. راجع `shared-components.css` للستايلات المتاحة
4. قارن مع صفحة المبيعات: [http://127.0.0.1:8000/sales/](http://127.0.0.1:8000/sales/)

---

## 📝 Changelog

### الإصدار 3.2 (2025-11-08) - دعم الأزرار الوظيفية
- ✅ **page_header.html:** إضافة دعم الأزرار الوظيفية مع `onclick`
- ✅ **أنواع الأزرار:** دعم 3 أنواع: تنقل (url)، وظيفي (onclick)، نموذج (form_id)
- ✅ **مثال عملي:** صفحة الإيرادات مع زر "إضافة إيراد" يفتح مودال
- ✅ **التوثيق:** تحديث أمثلة الاستخدام في `page_header.html`

### الإصدار 3.1 (2025-11-08) - تحديثات من الدروس العملية
- ✅ **قواعد الأزرار:** إضافة قسم كامل عن الالتزام بالهيدر الأصلي
- ✅ **أزرار المودال:** توضيح استخدام `toggle` و `target` للأزرار الوظيفية
- ✅ **CSS Guidelines:** توضيح ما يُحذف وما يُحفظ من CSS
- ✅ **خطأ reverse:** إضافة حل لمشكلة `NameError: name 'reverse' is not defined`
- ✅ **أمثلة عملية:** إضافة مثال كامل لصفحة الموظفين مع زر استيراد

### الإصدار 3.0 (2025-11-08) - النسخة الموحدة النهائية
- ✅ **دمج الأدلة:** دمج PAGE_MIGRATION_GUIDE و SHARED_COMPONENTS_GUIDE
- ✅ **مرجعية واحدة:** ملف واحد شامل لكل شيء
- ✅ **مثال حي:** إضافة رابط صفحة المبيعات كمرجع
- ✅ **CSS Variables:** التأكيد على استخدام المتغيرات فقط
- ✅ **تنظيف شامل:** إزالة التكرار والمعلومات القديمة

### الإصدار 2.0 (2025-01-08)
- ✅ دمج جميع الأدلة في ملف واحد
- ✅ Checklist شامل
- ✅ أمثلة عملية
- ✅ حلول للأخطاء الشائعة

### الإصدار 1.0 (2025-01-08)
- ✅ إنشاء النظام المركزي
- ✅ إضافة Breadcrumb مركزي
- ✅ إضافة Page Header مركزي
- ✅ إضافة Stats Cards مركزية
- ✅ توثيق شامل للاستخدام

---

**اتبع هذا الدليل لكل صفحة جديدة لضمان التوحيد الكامل!** 🚀
