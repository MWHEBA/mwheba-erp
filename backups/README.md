# 📦 نظام Backup للمشروع

## 🎯 الغرض
هذا المجلد يحتوي على نظام backup شامل للمشروع يتيح لك:
- حفظ حالة النظام الحالية
- استعادة النظام لحالة سابقة
- إدارة عدة نسخ احتياطية

## 📁 محتويات المجلد

### ملفات Backup:
- `db_backup_YYYYMMDD_HHMMSS.sqlite3` - نسخة من قاعدة البيانات
- `migrations_status_YYYYMMDD_HHMMSS.txt` - حالة الـ migrations
- `system_info_YYYYMMDD_HHMMSS.txt` - معلومات النظام

### ملفات الإدارة:
- `restore_backup.py` - سكريبت استعادة النظام
- `README.md` - هذا الملف

## 🚀 كيفية الاستخدام

### إنشاء Backup جديد:
```bash
python create_backup.py
```

### عرض قائمة الـ Backups:
```bash
python create_backup.py list
# أو
python backups/restore_backup.py list
```

### استعادة النظام من Backup:
```bash
python backups/restore_backup.py
```

## ⚠️ ملاحظات مهمة

1. **النظام يحتفظ بآخر 5 backups فقط** - الملفات القديمة تُحذف تلقائياً
2. **تأكد من إغلاق الخادم** قبل استعادة النظام
3. **الاستعادة تحذف قاعدة البيانات الحالية** - تأكد من إنشاء backup قبل الاستعادة

## 📊 معلومات الملفات

### قاعدة البيانات:
- الحجم الحالي: ~3.2 MB
- تحتوي على: جميع البيانات والإعدادات

### حالة Migrations:
- جميع الـ migrations مطبقة بنجاح
- لا توجد تضارب في التبعيات

## 🔄 سير العمل المقترح

1. **قبل أي تغيير جذري:**
   ```bash
   python create_backup.py
   ```

2. **تنفيذ التغييرات**

3. **في حالة وجود مشاكل:**
   ```bash
   python backups/restore_backup.py
   ```

4. **التحقق من سلامة النظام:**
   ```bash
   python manage.py check
   python manage.py showmigrations
   ```

## 📅 تاريخ آخر backup
تم إنشاء آخر backup في: 2025-11-11 19:27:36

---
💡 **نصيحة:** استخدم هذا النظام بانتظام خاصة قبل:
- تعديل الـ migrations
- تحديث النماذج
- تطبيق تغييرات جذرية
- نشر النظام
