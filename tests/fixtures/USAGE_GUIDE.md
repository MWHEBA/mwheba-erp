# دليل استخدام البيانات التجريبية

## نظرة عامة
تم إنشاء fixtures تجريبية تحتوي على 4 سيناريوهات كاملة للشراء والبيع مع الدفعات.

## السيناريوهات المتاحة

### 📦 السيناريو الأول: فاتورة شراء نقدي
```
رقم الفاتورة: PUR0001
التاريخ: 2025-01-15
المورد: شركة الورق السعودية
نوع الدفع: نقدي
حالة الدفع: مدفوعة بالكامل

البنود:
- كوشيه 300 جم: 500 فرخ × 5 ج = 2,500 ج

الدفعات:
- 2,500 ج نقداً (CASH-001)
```

### 📦 السيناريو الثاني: فاتورة شراء آجل مع دفعة جزئية
```
رقم الفاتورة: PUR0002
التاريخ: 2025-01-16
المورد: مطابع الخليج الرقمية
نوع الدفع: آجل
حالة الدفع: مدفوعة جزئياً

البنود:
- أوفست 80جم: 1,000 فرخ × 2.50 ج = 2,500 ج
- دوبلكس 250جم: 300 فرخ × 4 ج = 1,200 ج
الإجمالي: 3,700 ج

الدفعات:
- 2,000 ج نقداً (CASH-002)
المتبقي: 1,700 ج
```

### 🛒 السيناريو الثالث: فاتورة بيع نقدي
```
رقم الفاتورة: SALE0001
التاريخ: 2025-01-17
العميل: راقيات الإبداع
نوع الدفع: نقدي
حالة الدفع: مدفوعة بالكامل

البنود:
- كوشيه 300 جم: 100 فرخ × 7 ج = 700 ج

الدفعات:
- 700 ج نقداً (CASH-003)
```

### 🛒 السيناريو الرابع: فاتورة بيع آجل مع تحصيل جزئي
```
رقم الفاتورة: SALE0002
التاريخ: 2025-01-18
العميل: تراست بلس
نوع الدفع: آجل
حالة الدفع: مدفوعة جزئياً

البنود:
- أوفست 80: 200 فرخ × 3.5 ج = 700 ج
- دوبلكس 250: 50 فرخ × 5.50 ج = 275 ج
الإجمالي: 975 ج

الدفعات:
- 500 ج نقداً (CASH-004)
المتبقي: 475 ج
```

## 💰 الملخص المالي

### المشتريات
- إجمالي المشتريات: **6,200 ج**
- المدفوع للموردين: **4,500 ج**
- المتبقي للموردين: **1,700 ج**

### المبيعات
- إجمالي المبيعات: **1,675 ج**
- المحصل من العملاء: **1,200 ج**
- المتبقي من العملاء: **475 ج**

### صافي حركة الخزينة
- الوارد (من العملاء): **1,200 ج**
- الصادر (للموردين): **4,500 ج**
- **الصافي: -3,300 ج** (خروج نقدية)

## 🚀 طريقة التحميل

### الطريقة الأولى: استخدام السكريبت (موصى بها)
```bash
python load_demo_data.py
```

### الطريقة الثانية: أوامر Django مباشرة
```bash
# تحميل كل شيء دفعة واحدة
python manage.py loaddata users/fixtures/initial_data.json product/fixtures/initial_data.json supplier/fixtures/initial_data.json client/fixtures/initial_data.json financial/fixtures/chart_of_accounts_final.json tests/fixtures/demo_transactions.json
```

### الطريقة الثالثة: خطوة بخطوة
```bash
python manage.py loaddata users/fixtures/initial_data.json
python manage.py loaddata product/fixtures/initial_data.json
python manage.py loaddata supplier/fixtures/initial_data.json
python manage.py loaddata client/fixtures/initial_data.json
python manage.py loaddata financial/fixtures/chart_of_accounts_final.json
python manage.py loaddata tests/fixtures/demo_transactions.json
```

## 🔍 التحقق من البيانات

### من خلال Django Shell
```python
python manage.py shell

# عرض فواتير الشراء
from purchase.models import Purchase, PurchasePayment
purchases = Purchase.objects.all()
for p in purchases:
    print(f"{p.number}: {p.supplier.name} - {p.total} ج - {p.get_payment_status_display()}")
    print(f"  المدفوع: {p.amount_paid} ج")
    print(f"  المتبقي: {p.amount_due} ج")

# عرض فواتير البيع
from sale.models import Sale, SalePayment
sales = Sale.objects.all()
for s in sales:
    print(f"{s.number}: {s.customer.name} - {s.total} ج - {s.get_payment_status_display()}")
    print(f"  المحصل: {s.amount_paid} ج")
    print(f"  المتبقي: {s.amount_due} ج")
```

### من خلال واجهة الإدارة
1. افتح المتصفح على: `http://localhost:8000/admin/`
2. سجل دخول بالمستخدم: `mwheba` / `MedooAlnems2008`
3. تصفح:
   - Purchase → Purchases (فواتير الشراء)
   - Sale → Sales (فواتير البيع)
   - Purchase → Purchase payments (دفعات الشراء)
   - Sale → Sale payments (دفعات البيع)

## 📊 حالات الاختبار المتاحة

### ✅ يمكن اختبار:
1. **فواتير نقدية مدفوعة بالكامل** (PUR0001, SALE0001)
2. **فواتير آجلة مدفوعة جزئياً** (PUR0002, SALE0002)
3. **حساب الأرصدة المستحقة** (للموردين والعملاء)
4. **حركة الخزينة** (الوارد والصادر)
5. **تقارير المشتريات والمبيعات**
6. **حالات الدفع المختلفة** (paid, partially_paid)

## 🔧 حذف البيانات التجريبية

إذا أردت حذف البيانات وإعادة التحميل:

```bash
# حذف الفواتير والدفعات
python manage.py shell
>>> from purchase.models import Purchase, PurchasePayment
>>> from sale.models import Sale, SalePayment
>>> PurchasePayment.objects.all().delete()
>>> SalePayment.objects.all().delete()
>>> Purchase.objects.all().delete()
>>> Sale.objects.all().delete()
>>> exit()

# إعادة التحميل
python load_demo_data.py
```

## ⚠️ ملاحظات مهمة

1. **الترتيب مهم**: يجب تحميل البيانات بالترتيب الصحيح لتجنب أخطاء Foreign Key
2. **الحساب المالي**: جميع الدفعات مربوطة بحساب الخزينة الرئيسية (كود: 11011)
3. **حالة الترحيل**: جميع الدفعات في حالة "posted" (مرحّلة)
4. **التواريخ**: البيانات بتواريخ يناير 2025
5. **المستخدم**: جميع العمليات منسوبة للمستخدم `mwheba` (ID: 1)

## 📝 تخصيص البيانات

يمكنك تعديل ملف `tests/fixtures/demo_transactions.json` لتغيير:
- التواريخ
- الكميات
- الأسعار
- الموردين/العملاء
- حالات الدفع

بعد التعديل، أعد تحميل البيانات:
```bash
python load_demo_data.py
```

## 🆘 المساعدة

إذا واجهت أي مشاكل:
1. تأكد من تشغيل السيرفر: `python manage.py runserver`
2. تحقق من قاعدة البيانات: `python manage.py migrate`
3. راجع ملف `README.md` في نفس المجلد
4. تحقق من الأخطاء في Console
