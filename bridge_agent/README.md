# Biometric Bridge Agent

وكيل آمن يربط ماكينة البصمة ZKTeco بالسيرفر - يعمل كخدمة ويندوز في الخلفية

## التثبيت التلقائي (3 خطوات فقط!)

### 1. تحميل Bridge Agent
- حمّل الملف من النظام (صفحة الجهاز → إعداد Bridge Agent)
- استخرج الملفات في مكان ثابت (مثلاً: `C:\BridgeAgent`)

### 2. تثبيت كخدمة ويندوز
```bash
# كليك يمين على install_service.bat → Run as administrator
install_service.bat
```

**السكريبت سيقوم بكل شيء تلقائياً:**
- ✅ تحميل وتثبيت NSSM (أداة الخدمة)
- ✅ التحقق من Python وتثبيت المكتبات المطلوبة
- ✅ تثبيت وتشغيل الخدمة
- ✅ ضبط التشغيل التلقائي مع الويندوز

### 3. تم! 🎉
الخدمة الآن تعمل في الخلفية وتزامن البيانات كل 5 دقائق

---

## أوامر إدارة الخدمة

```bash
# تشغيل الخدمة
nssm start BiometricBridge

# إيقاف الخدمة
nssm stop BiometricBridge

# إعادة تشغيل الخدمة
nssm restart BiometricBridge

# حالة الخدمة
nssm status BiometricBridge

# إلغاء تثبيت الخدمة
uninstall_service.bat
```

---

## ملفات السجلات

```
C:\BridgeAgent\
├── service.log          ← سجل التشغيل
├── service_error.log    ← سجل الأخطاء
└── bridge_agent.log     ← سجل المزامنة
```

---

## الإعدادات على السيرفر

```python
# في .env
BRIDGE_AGENTS=AGENT-001:your-secret-key-here

# أو في settings.py
BRIDGE_AGENTS = {
    'AGENT-001': 'your-secret-key-here'
}
```

---

## المتطلبات

- ✅ Windows 7 أو أحدث
- ✅ Python 3.8 أو أحدث (يتم التحقق تلقائياً)
- ✅ اتصال بالإنترنت (للتثبيت فقط)
- ✅ صلاحيات Administrator (للتثبيت فقط)

---

## المكتبات المستخدمة

- `pyzk` - للاتصال بأجهزة ZKTeco
- `requests` - للاتصال بالسيرفر
- `schedule` - لجدولة المزامنة

---

## الأمان

- 🔒 مفاتيح سرية مشفرة
- 🔒 اتصال HTTPS مع السيرفر
- 🔒 التحقق من صحة البيانات
- 🔒 سجلات مفصلة للمراجعة
